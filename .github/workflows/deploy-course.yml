name: deploy-course

# 客户使用教程静态站（course/ 目录）:普通推送即重建发布到 GitHub Pages。
# 与 build-deb 互不影响:deb 只由 workflow_dispatch 派发,本工作流只监听 push。
on:
  push:
    branches: [main]
    paths:
      - "course/**"
      - ".github/workflows/deploy-course.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install deps
        run: pip install markdown
      - name: Build site
        run: python3 course/build_site.py --out dist
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
